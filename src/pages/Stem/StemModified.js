import React from "react";
import { withStyles } from "@material-ui/core/styles";

const styles = theme => ({

});

function StemModified(props) {
    return <div />;
}

export default withStyles(styles)(StemModified);